#ifndef MAP_H
#define MAP_H

#include <irrlicht.h>

class Map{
    public:
        Map(irr::scene::ISceneManager *smgr, irr::video::IVideoDriver *driver, irr::scene::IMetaTriangleSelector *selectorGlobal);
    private:
};

#endif // MAP_H
